# Slack Alert Configuration

Configure a Slack API credential in n8n and select the target channel using SLACK_ALERT_CHANNEL.

Never place the token in workflow JSON, README, screenshots, or Git.

The monitoring alert should include target URL, HTTP status, timestamp, execution ID, and an instruction to investigate.